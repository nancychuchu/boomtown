const styles = theme => ({
  formControl: {
    marginBottom: theme.spacing.unit * 2,
    width: '100%'
  },

  button: {
    width: '60%'
  },

  textInput: {
    width: '60%'
  }
});

export default styles;
