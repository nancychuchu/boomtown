const styles = theme => ({
  formControl: {
    width: '100%'
  },

  button: {
    width: '60%',
    backgroundColor: theme.palette.primary.main
  },

  textInput: {
    width: '60%'
  }
});

export default styles;
